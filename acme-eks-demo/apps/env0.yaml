version: 2

deploy:
  steps:
    helmDiff:
      before:
        - name: Configure kubeconfig
          run: aws eks update-kubeconfig --name acme-financial-eks --region us-east-2

    helmDeploy:
      before:
        - name: Configure kubeconfig
          run: aws eks update-kubeconfig --name acme-financial-eks --region us-east-2
