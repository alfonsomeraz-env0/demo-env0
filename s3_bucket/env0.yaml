version: 2

deploy:
  steps:
    setup:
      after:
        - name: Print custom message
          run: |
            echo "Starting env0 deployment"
            echo "This is a custom script running in env0"
            echo "Environment: $ENV0_ENVIRONMENT_NAME"
            echo "Deployment ID: $ENV0_DEPLOYMENT_ID"
            echo "Done"
